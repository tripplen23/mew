//! Golden task reference implementations.
//!
//! Each module implements a deterministic behavior ported from a reference
//! source (typically Python). Used by the mew-migration skill pack as baseline
//! targets during golden task verification runs.
//!
//! See `tests/fixtures/golden-task-1/` for full run artifacts.

pub mod wc;
